<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mob204_duanmau">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".UI.DeleteBill"
            android:parentActivityName=".UI.Home"
            android:theme="@style/ThemeNoActionBar">
        </activity>

        <activity android:name=".UI.ManagerBill"
            android:parentActivityName=".UI.Home"
            android:theme="@style/ThemeNoActionBar"/>

        <activity android:name=".UI.ManagerUser"
            android:parentActivityName=".UI.Home"
            android:theme="@style/ThemeNoActionBar"/>

        <activity android:name=".UI.ManagerNumberBook"
            android:parentActivityName=".UI.Home"
            android:theme="@style/ThemeNoActionBar"/>

        <activity
            android:name=".UI.BookManeger"
            android:parentActivityName=".UI.Home"
            android:theme="@style/ThemeNoActionBar" />
        <activity android:name=".UI.Home" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.SignUp" />
        <activity android:name=".UI.Login" />
        <activity android:name=".UI.MainActivity">

        </activity>
    </application>

</manifest>